summary: produce again
tasks:
    - id: kafka_test_call
      fn: com.gs.kafka
      args:
        datasource: my-kafka
        config:
          method: publish
          topic: notify-delivery
        data:
          value: 'Hello, i got the message, I am forwarding, I am coming from Kafka.'
